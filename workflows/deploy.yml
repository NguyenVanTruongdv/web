name: Deploy static website to Somee

on:
  push:
    branches:
      - main  # Tự động deploy khi push lên nhánh main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Somee via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: 155.254.244.28
          username: namroi
          password: Truongnv2410@
          protocol: ftps
          local-dir: ./   # thư mục chứa file index.html, css, js,...
          server-dir: /www.nguyenvantruong.somee.com/
          timeout: 600
